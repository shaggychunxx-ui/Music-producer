Runtime lives in the package:

```
python -m clip_edit_kb extract-audio CLIP.mp4
python -m clip_edit_kb prepare-s1 CLIP.mp4 --song-dir SONG_DIR
```

Studio-One DAW helper (same 48 kHz sidecar, no PYTHONPATH to this package):

```
Documents\GitHub\Studio-One\tools\prepare_video_for_s1.py
```
